{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}

<section class="formulaire">
<form action=" {{ path ("app_login") }} " method="post">

    {# {% if app.user %}
        <div class="">Vous êtes connecté en tant que {{ app.user.pseudo }}, <a href="{{ path('app_logout') }}">Déconnexion</a></div>
    {% endif %} #}

    <div class="saisieform">
        <div class="idconnex">
            <label for="inputEmail" class="">Courriel</label>
            <input type="text" required name="_username"  class="" placeholder="Courriel">
        </div>
        <div class="idconnex">
            <label for="inputPassword" class="">Mot de passe</label>
            <input type="password" required name="_password"  class="" placeholder="Mot de passe">
        </div>
    

    {# <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"> #}
    

    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}

        <div>
            <button type="submit" class="valider2">Connexion</button>
        </div>
    </div>

</form>
</section>

{% endblock %}
